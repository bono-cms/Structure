
<div class="row mb-4 mt-4 align-items-center">
    <div class="col-lg-6">
        <h4>
            <i class="fas fa-table"></i> <?= $this->translate('Manage structures'); ?> <small class="text-muted">(<?= count($collections); ?>)</small>
        </h4>
    </div>

    <?php if (!empty($collections)): ?>
    <div class="col-lg-6 text-lg-right">
        <a class="btn btn-danger py-3 px-4 rounded-3" href="<?= $this->url('Structure:Admin:Dashboard@flushAction'); ?>"><?= $this->translate('Flush the cache'); ?></a>
    </div>
    <?php endif; ?>
</div>

<div class="row">
    <?php foreach ($collections as $collection): ?>
    <div class="col-lg-3">
        <div class="p-4 border text-center mb-3 shadow-lg bg-white">
            <span class="d-block mb-2">
                <i class="module-icon fas fa-table"></i>
            </span>
            <h4><?= $collection['name']; ?></h4>

            <p>
                <small><strong><?= $collection['count']; ?></strong> <?= $this->translate('fields'); ?>, ID = <strong><?= $collection['id']; ?></strong></small>
            </p>
            <p>
                <a href="<?= $this->url('Structure:Admin:Repeater@indexAction', $collection['id']); ?>"><i class="fas fa-pencil-alt"></i> <?= $this->translate('Add or edit records'); ?></a>
            </p>
        </div>
    </div>
    <?php endforeach; ?>

    <!-- Add new -->
    <div class="col-lg-3">
        <div class="p-3 border text-center h-100 d-flex flex-column align-items-center justify-content-center">
            <span class="d-block mb-2">
                <i class="module-icon fas fa-sliders-h"></i>
            </span>

            <h5>
                <a href="<?= $this->url('Structure:Admin:Collection@indexAction'); ?>"><?= $this->show('Manage structures'); ?></a>
            </h5>
        </div>
    </div>
</div>

<style>
.module-icon {
    font-size: 38px;
}
</style>